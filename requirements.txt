# Core dependency for shared/schemas.py (Pydantic v2 REST payload models).
pydantic>=2.7,<3

# FastAPI web framework and ASGI server (needed by cloud_x, middle_y, end_z).
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

# Async HTTP client (needed by middle_y, end_z for inter-tier calls).
httpx>=0.28,<1

# GUI dashboard
streamlit>=1.40,<2
plotly>=5,<6
pandas>=2,<3

# Added in later phases (tests):
# pytest
